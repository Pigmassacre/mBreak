# TODOs

## The lightning effect should be replaced

* Instead of the current effect it should cause a chain lightning attack when a ball with this effect hits a block.
* Currently it causes damage to blocks in a rect around it. This should be replaced with it selecting a random block around it in a larger rect, causing damage to that, then repeating the effect from that new block 4-5 times, dealing slightly less damage each time.
* This effect should be represented visually by drawing lines between all the damaged blocks.

## The laser effect is too strong when fully charged

* It should not last as long.